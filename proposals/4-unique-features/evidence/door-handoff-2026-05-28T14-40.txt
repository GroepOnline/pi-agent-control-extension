This door (fresh 10 door / iter37):

- Created RUN_DIR artifacts/runs/2026-05-28T14-35-00-tuistory-official-onramp-validation-iter37/ with commitments.md pre-touch (4 claims focused on production readiness of official bin/control-narrate --render --props path with real 4-chapter props, original atoms, guards reference, color rule, verification gate).
- Recipe-compliant PTY captures (asciinema + script(1) recovery with exact 120x36 + FORCE_COLOR=3 + COLORTERM=truecolor; tctl at bin/tctl with tuistory backend syntax confirmed and matching recipe): doctor, skills list (original atoms tuistory/control-narrate/pi-agent-control/verify/compose/ralph/capture/pty-capture etc. preserved), control-narrate --help, npm run validate, official on-ramp matrix (happy path + 2 error cases), guards baseline + color rule.
- Official on-ramp exercised: bypass ("Using provided --props file directly"), realpath polish, corrected syntax (cd remotion + PI_CLI_THEME + color envs + npm run still -- src/index.ts NarratorShowcase --props <absolute real-improved>), reached Composition NarratorShowcase + "render attempted". All under recipe env. Evidence in official-onramp-still-png.cast + error casts.
- proof-report.md: 4x PROVEN (ties to specific .cast excerpts, bin lines 142-154, guards 28-33, help docs, real props, recovery, 0-diff). Honest note on this-run render output (invocation validated; same props/syntax produced real PNG/MP4 in prior iter31).
- Final verification: npm run validate / doctor / lint all EXIT 0 (11/17 contract clean); guards-diff.txt 0 bytes vs baseline (all 8 sacred blocks incl. tuistory color rule pristine).
- Updated 10door-index.txt + extended feature1-guard-enforcement-proofs.md with cross-ref (iter37 on-ramp validation, 0-diff, NarratorShowcase wiring via official entrypoint, color rule fidelity, proof-report evidence).
- recovery.md + guards-color-rule.txt + bin pre-capture snapshot + real props copy + verify/ outputs self-contained in RUN_DIR.

Key result: The official governed on-ramp (bin/control-narrate with all prior polish + wiring to NarratorShowcase) has been systematically validated under the exact tuistory recipe as production-ready for driving cinematic output from real verification bundles while the 8 sacred guards (especially tuistory color at 28-33) and full contract remain 0-delta.

Next door options (updated from prior handoff + this validation):
1. Polish / expand the NarratorShowcase using the proven pipeline (add more EffectLayer/TransitionLayer/TitleCard reuse, consume more real-props fields like transitions/overlays/keystrokes/codeAnnotations, richer chapter body content for the 4 features).
2-3. More official surface captures (additional tuistory or other driver traces exercising bin/control-narrate + skill-studio TUI + pi-agent-control subcommands under recipe; more guard cross-ref updates).
4. Wiring polish / edge cases for the on-ramp (e.g. full MP4 render variant, better error surfacing for Remotion failures, integration against fresh verify bundles).

All under exact recipe env. Kernel untouched (0-byte guards diffs every door). Original skill atom names preserved in all transcripts.

Loop continues.
