This door:
- Executed a full tuistory recipe proof-report capture exercising the complete pipeline (iter22) per the exact supplied recipe.
- Created RUN_DIR artifacts/runs/2026-05-28T11-28-39-tuistory-pipeline-proof-iter22 with commitments.md recorded before any capture.
- Performed pre-invocation and post-invocation recipe-compliant PTY captures (FORCE_COLOR=3 + COLORTERM=truecolor + 120x36), including a dedicated invocation capture of the exact npx remotion still command against the expanded NarratorShowcase + real 4-feature props from the 2026-05-28T07-00-46 bundle.
- The invocation exercised the iter21 expansion (Keystrokes now active for the "keystroke-pills" effect in the props; CodeAnnotations for the guard rule) via the iter20-wired bin/control-narrate path.
- Produced pre/invocation/post snapshots + .casts + guards-diff-iter22.txt (0 bytes vs iter21) + proof-report.md with 4x PROVEN + gate PASS.
- Appended iter22 line to evidence/10door-index.txt and extended the Feature 1 guard enforcement proofs cross-ref.

Strongest new material:
- End-to-end proof that the full Feature 4 pipeline (verify → control-narrate wiring → expanded NarratorShowcase with Keystrokes/CodeAnnotations for the 4 features) is invocable and exercised with real loop-generated props under the exact tuistory recipe.
- Complete before/during/after evidence under the recipe (pre = post-iter21 state; invocation = expanded code paths active; post = no kernel change).
- Another 0-byte guards diff confirming the kernel stayed immutable while closing the pipeline loop.

Next door options (prioritized):
1. Re-run the exact still/render commands (now with the expanded NarratorShowcase containing Keystrokes/CodeAnnotations) via the wired `bin/control-narrate --render` in an environment where Remotion produces actual PNG/MP4 output from real 4-chapter props.
2. Further expand NarratorShowcase (more EffectLayer/TransitionLayer/TitleCard reuse, consume real props keystrokes/codeAnnotations fields, better chapter body content).
3. Polish the iter20 wiring (props path detection, video vs still, integration tests against real verification bundles).
4. More recipe captures of the expanded surface, updated skills list, or `bin/control-narrate --render --help`.

All under exact recipe env. Kernel untouched. Original skill atom names preserved in all transcripts.

Loop continues.