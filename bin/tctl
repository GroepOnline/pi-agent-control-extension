#!/usr/bin/env bash
# bin/tctl — Thin entrypoint for tctl terminal control
# Delegates directly to the bash script in src/drivers/terminal/tctl.sh

set -euo pipefail

ROOT="$(cd "$(dirname "$0")/.." && pwd)"
exec "$ROOT/src/drivers/terminal/tctl.sh" "$@"
